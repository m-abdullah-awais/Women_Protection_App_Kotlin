<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_card"
    android:layout_marginBottom="8dp"
    android:padding="10dp"
    android:elevation="2dp"> 
    <!-- Reduced padding from 20dp to 10dp. Reduced margin from 12dp to 8dp. -->

    <!-- Selection Checkbox - Far Left - Custom Tick -->
    <CheckBox
        android:id="@+id/cbSelect"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:button="@drawable/selector_checkbox_custom"
        android:buttonTint="@null" 
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
        <!-- Removed Tint so it uses the drawable colors directly (Red Tick, Grey Circle) -->

    <!-- Divider Line -->
    <View
        android:id="@+id/vDivider"
        android:layout_width="1dp"
        android:layout_height="32dp"
        android:background="#22FFFFFF"
        android:layout_marginStart="12dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/cbSelect"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Name and Number Stack -->
    <!-- Adjusted Margins for tighter layout -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp"
        android:orientation="vertical"
        android:gravity="center_vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/spinnerPriority"
        app:layout_constraintStart_toEndOf="@+id/vDivider"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/tvName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="John Doe"
            android:textColor="@color/text_primary"
            android:textSize="18sp" 
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />
            <!-- Reduced Size slightly from 20 to 18 to fit "minimized" request? 
                 User asked to "minimize padding", not specifically size. 
                 But tight padding with huge text looks weird. keeping 18sp seems balanced. -->

        <TextView
            android:id="@+id/tvNumber"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="+1 (555) 000-0000"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <!-- Priority Badge - Right Aligned -->
    <Spinner
        android:id="@+id/spinnerPriority"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:background="@drawable/bg_spinner_custom"
        android:overlapAnchor="false"
        android:minWidth="80dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
